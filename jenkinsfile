node {
    options {
        skipDefaultCheckout()  This is the key condition for scripted
    }
    stage('No Checkout Stage') {
        echo 'This stage runs without a default SCM checkout.'
         Custom logic here
    }
     If you need to checkout later
    stage('Manual Checkout Stage') {
        checkout scm
    }
}